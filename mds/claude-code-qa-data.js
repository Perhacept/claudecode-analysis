window.CC_WALKTHROUGH_QA_GROUPS = {
  "harness-why": {
    "title": "为什么先学 Harness，而不是直接读模型调用",
    "ref": "问题组 1.1",
    "answerSource": "notebookAns.md",
    "questions": [
      "用 300 字以内解释 Claude Code 源码里 Harness Engineering 的三大支柱分别解决什么问题。",
      "为什么强调投资 Harness，而不是只依赖提示词或微调？请结合 Claude Code 的工具、权限、上下文管理举例。",
      "新手读源码时，怎样区分“模型能力”和“工程线束能力”？",
      "源码中处处可见为了 Prompt Caching 和性能做出的“反直觉”设计。能否给出几个具体的量化数据，证明这种“算账式”架构的必要性？"
    ],
    "answers": [
      "Harness Engineering（线束工程）的三大支柱就像是给AI“配眼镜、建围栏、搞后勤”，具体解决以下问题：\n\n**上下文工程（配眼镜）**：解决“AI应该知道什么，怎么防止被信息撑爆”的问题。大模型视野有限且按Token收费，源码通过动静分离的缓存架构、动态注入 `CLAUDE.md`，以及四级递进的压缩管道（Compact），确保AI始终看到最核心的上下文，同时防止长对话把API预算烧光。**架构约束（建围栏）**：解决“怎么防止AI失控”的问题。系统不靠软性的提示词去“建议”AI，而是用硬代码划定边界。通过 40多个严格定义输入格式的工具、6层纵深防御的权限漏斗（从正则匹配到系统沙盒），在物理层面拦截诸如 `rm -rf` 这类危险操作。**熵管理（搞后勤）**：解决“项目久了越跑越乱”的问题。长周期下代码和文档容易退化，系统通过定时任务（如依赖审计）和文档验证Agent来对抗老化的“熵增”，保障长期稳定性。",
      "强调投资 Harness，是因为**微调模型需要数月时间和高昂算力，且与特定模型强绑定；而单纯依靠写提示词（Prompt）去约束 AI 根本靠不住，大模型随时可能产生幻觉并忽略规则**。Harness 是用传统软件工程的严谨去“硬控”不确定的 AI：\n\n**工具管理的例子**：如果只靠提示词，模型可能会胡乱瞎编 API。但在 Claude Code 里，工具（Tools）是硬接口，默认设为不可并行（`isConcurrencySafe: false`）、非只读，AI 只能在这个保守的物理边界内调用，甚至 FileEdit 必须先读过文件才能编辑，从根源掐断幻觉。**权限管理的例子**：不用提示词警告 AI“别碰敏感文件”，源码直接建立了 6 层递进的安全防御。即使你在提示词里让它删除 `.git` 或系统文件，底层的沙盒（Sandbox）和硬编码拒绝（Hardcoded Denials）也会让这个操作百分之百失败，安全遵守率达到绝对的 100%。**上下文管理的例子**：一味堆提示词会把长对话成本拉爆。Harness 引入了未公开的黑科技 `Cache Edits`（增量删除缓存），并在遇到超长 Token 时采用四层自动压缩。它甚至把 Prompt 拆成可跨用户共享的“静态全局缓存”和不缓存的“动态注入数据”，直接在架构层面把 API 成本和延迟降到最低。",
      "新手在读源码时，可以通过**“是否发生在 API 请求边界内”**来简单区分两者。可以将“模型能力”看作是发动机，而“工程线束能力”是外围的底盘、刹车和仪表盘。\n\n**如何找“模型能力”**：当代码在构建 payload、向 Anthropic API 发起请求，或者利用大模型去推理判断时，就是在使用模型能力。比如：让 `Claude Opus` 分析代码报错原因、生成一段补丁代码，或者使用小模型 `Sonnet` 作为裁判去给历史记忆打分挑选（Active Recall）。模型只负责“动脑思考”并输出文本或 JSON 格式的工具调用意图。**如何找“工程线束能力（Harness）”**：看那些包裹在 API 调用外围、为 AI 擦屁股和跑腿的代码。比如：`src/query.ts` 里的 `while(true)` 死循环和 7 个错误恢复点：当 API 返回 413 过长错误时，系统不让人类去处理，而是用线束代码自动执行折叠重试。工具的并发执行流 `StreamingToolExecutor`：模型说要读 10 个文件，线束把它们分成并发批次，几毫秒内同时读完，这是工程线束在压榨 I/O 性能。后台提取记忆：对话结束后，线束会 Fork 出一个子 Agent 偷偷在后台读聊天记录写 Markdown 笔记，而完全不阻塞用户的屏幕。 简而言之：模型只负责输出指令，凡是涉及**并发控制、本地文件读写、权限弹窗拦截、历史消息数组的裁剪与压缩**，全都是工程线束（Harness）的能力。",
      "Claude Code 的很多架构妥协，都是被极其高昂的 Token 成本和物理资源限制逼出来的。以下三个真实数据最能说明问题：\n\n* **探索 Agent 的百亿 Token 裁剪**：内置的 Explore 子 Agent 主要负责只读搜索。源码在其 System Prompt 中显式移除了 `CLAUDE.md` 注入和 Git 状态获取。这个不起眼的上下文裁剪，在面对每周 3400 万次调用时，节省了高达 50~150 亿 Token（5-15 Gtok）的 API 成本。\n* **325MB 内存开销引发的 InProcess 协议**：在接入 Chrome 浏览器控制这类重量级 MCP 服务时，如果使用常规的 `stdio` 启动独立的 Node.js 子进程，会产生高达 ~325MB 的内存开销和严重的冷启动延迟。为此，系统专门设计了 `InProcess` 协议，直接在主进程内存中加载模块，从物理上消除了 IPC 开销。\n* **90% 的缓存读折扣**：如果不使用 Prompt Caching 冻结静态前缀，一个 100 轮的长代码会话可能耗费 50-100 美元的输入成本。通过死保 `SYSTEM_PROMPT_DYNAMIC_BOUNDARY` 前的稳定字节，成本被极限压缩到了 10-19 美元左右。"
    ]
  },
  "startup-flow": {
    "title": "启动、Prompt 与消息规范化",
    "ref": "问题组 2.1",
    "answerSource": "notebookAns.md",
    "questions": [
      "请按从用户输入到 API 请求的顺序，拆解 Claude Code 的启动与消息规范化链路，重点解释 system prompt boundary 和 normalizeMessagesForAPI 的职责。"
    ],
    "answers": [
      "整个 Claude Code 的启动与消息入模链路，是一套非常精密的数据处理流水线。从你敲下回车键开始，数据会经历以下四个核心阶段的流转与清洗：\n\n1. 用户输入与消息规范化（`normalizeMessagesForAPI`）\n\n当终端接收到用户输入后，它首先被构建为一条 User Message（用户消息）送入 `QueryEngine`。在发送给大模型 API 之前，系统会调用 `normalizeMessagesForAPI` 执行一条复杂的预处理管线。 在这里，系统针对不同类型的消息做了针对性的清洗：\n\n- System Prompt 拼接与 `SYSTEM_PROMPT_DYNAMIC_BOUNDARY` 的意义\n\n在清洗完消息后，系统开始组装给模型看的“世界观”——System Prompt。Claude Code 不会把一整块纯文本丢给模型，而是构建了一个包含多段配置的数组。 在这个数组中间，系统硬插入了一个极其关键的标识符：**SYSTEM_PROMPT_DYNAMIC_BOUNDARY**。 它的存在只有一个目的：**将 Prompt 物理劈开，实现动静两层缓存区域的彻底隔离**。 长对话中，每次交互都要发送几万 Token 的系统规则，成本极为高昂。通过这个分界线，系统向云端下达了明确的指令：“无论分界线后面的内容怎么变，分界线前面那些几千 Token 的前缀是绝对稳定的，请永远给我命中缓存！” 这使得 Claude Code 在处理上百轮长对话时，不仅延迟极低，而且每次 API 调用的前置成本被压缩到了极限。\n\n3. 静态 Prompt 与动态 Prompt 的缓存分配\n\n基于上述分界线，系统将不同的内容精确分配到了适合它们的缓存域中：\n\n- 这套设计如何服务“长会话稳定性”与“隐私边界”\n\n**捍卫长会话稳定性**：如果没有 `normalizeMessagesForAPI` 剔除无用的重复巨型 PDF 媒体和零碎的虚拟消息，没有动静隔离缓存大幅砍掉 System Prompt 的高频重算成本，一次几十轮的代码调试就会耗尽上下文窗口并产生极度高昂的账单。这种极限的 Token 预算管理与缓存优化是 Agent 能长时间稳定执行任务的根基。**筑起坚固的隐私边界**：为了保护 `scope: 'global'` 级别的静态共享缓存不被破坏，Claude Code 建立了一个铁律——**“规则放 System Prompt，数据放 User Message”**。这就解释了为什么包含你个人偏好、项目架构规范的 `CLAUDE.md`，以及包含你私有知识的记忆索引 `MEMORY.md` 绝对不能拼接进 System Prompt。系统巧妙地将这些高隐私的庞大数据用 `<system-reminder>` XML 标签妥善包裹，伪装成整个对话流中的**第一条用户消息（User Message）**动态注入进去。这样既让大模型掌握了你的私有项目背景，又在物理层面实现了数据隐私与全局共享缓存的完美隔离。"
    ]
  },
  "agent-loop-core": {
    "title": "queryLoop 为什么是心脏",
    "ref": "问题组 3.1",
    "answerSource": "notebookAns.md",
    "questions": [
      "请把 queryLoop 讲成一个状态机：它保存哪些状态、每轮循环做什么、为什么用 async generator，以及这些设计怎样支撑工具调用和恢复路径。",
      "在 Agent Loop 运行中，如果 API 突然返回 413 (Prompt Too Long) 或者用户上传了一张导致体积超限的巨型图片，系统会直接崩溃报错吗？"
    ],
    "answers": [
      "在 Claude Code 的底层架构中，`queryLoop` 绝对不是一个简单的 `while` 循环或有限重试逻辑，而是一个**具有 7 种恢复路径和 10 种终止条件的隐式状态机**。通过将其设计为状态机，系统能够在极度复杂的长对话和工具调用中保持逻辑的健壮性。\n\n结合源码，以下是针对该状态机架构的深度总结：\n\n1. 为什么使用 `async generator`（异步生成器）？\n\n`queryLoop` 签名返回 `AsyncGenerator`，这一设计从根本上改变了函数的行为模式，主要带来三大不可替代的优势：\n\n- `State` 中保存了哪些跨轮变量？\n\n为了彻底消除散落各处的局部变量导致的状态遗漏 Bug，`queryLoop` 的内部维护了一个极其严格的单一 `State` 结构体。该结构体在跨轮次迭代中保存了以下核心上下文：\n\n- 每轮循环大致经历哪些阶段？\n\n每次 `while(true)` 的迭代代表一次“API 调用 + 工具执行”的状态流转。其核心管道按顺序经历如下阶段：\n\n1. 工具结果、消息追加、恢复路径如何回到下一轮？\n\n`queryLoop` **绝对不使用深层嵌套的** **if-else** **或内层重试循环**，而是通过 **7 个 Continue 站点（Continue Sites）** 作为状态机的跃迁节点。\n\n- 这种结构对终端 UI、工具执行和错误恢复的巨大好处\n\n**对终端 UI 的好处（流式渲染与拦截）：** 因为使用了异步生成器，底层状态机可以频繁向外抛出 `StreamEvent`。React / Ink UI 层不需要干等整个任务结束，而是能实现像进度条、工具执行状态这样 60fps 的增量屏幕刷新。**对工具执行的好处（极限并发与打断）：** 生成器架构天然支持 `StreamingToolExecutor` 边接收 API 流边执行工具。如果用户中途按下 `Escape`，`.return()` 语义会瞬间掐断当前流，但完美保留已被写入 `State.messages` 的合法历史上下文，实现“零成本打断”。**对错误恢复的好处（扣留与降级保护）：** 7 个维度的恢复站点构建了一个极具弹性的系统。当遇到报错（如 413 Token 溢出或媒体图片过大）时，状态机会“扣留（withhold）”这个错误，不立即向外 `yield`。它会内部触发压缩管道（如 Context Collapse 或 Reactive Compact），释放空间后再通过 `continue` 重试。只有当所有恢复尝试（如重试 3 次）全部耗尽时，错误才会被暴露给 UI。这种设计保护了下游 SDK 消费者不会因为中间态的瞬时错误而错误地终止会话。",
      "绝对不会。生产级 Agent 不能遇到一次中间态错误就崩溃，Claude Code 在这里采用了非常精细的**错误扣留（Error Withholding）**与**反应式压缩（Reactive Compact）**机制：\n\n* **绝不外抛中间态错误**：如果 413 或超载错误被直接抛给外层 SDK 消费者（如 VS Code 扩展或桌面端），上游系统会立刻终止整个会话。因此，Agent Loop 会在内部强制扣留这个报错，对用户侧保持静默。\n* **触发局部剥离与无缝重试**：扣留错误后，系统在后台触发 Reactive Compact。它会精准执行尾部渐进剥离，比如自动找出导致媒体超限的那张图片或 PDF 块并将其剔除，或者排空折叠队列腾出空间。随后，系统带着 `hasAttemptedReactiveCompact` 标记无缝重新发起请求。用户看到的通常只是响应稍微慢了一点，而不是一次灾难性的崩溃。"
    ]
  },
  "context-cache": {
    "title": "上下文压缩与缓存",
    "ref": "问题组 4.1 / 4.2",
    "answerSource": "notebookAns.md",
    "questions": [
      "请详细解释 Claude Code 的四层上下文压缩管线：snip、microcompact、context collapse、autocompact 分别解决什么问题，为什么顺序不能随意交换。",
      "Cache Edits 与普通压缩有什么不同？它为什么强调不直接改本地 messages？"
    ],
    "answers": [
      "为了解决 AI Agent 在长对话中面临的“超长上下文导致 API 成本飙升与响应延迟”这一根本矛盾，Claude Code 在底层架构中设计了一条极其精密的**四层递进式上下文压缩管线（Compaction Pipeline）**。\n\n对于源码学习者而言，这不仅仅是“删减聊天记录”，而是一套结合了缓存命中优化、读时投影机制和渐进式降级的工业级解决方案。以下是详细拆解：\n\n1. 每一层解决什么问题？\n\n这四层管线在每次 API 调用前按顺序执行，层层递进：\n\n- 为什么它们按“先轻后重”的顺序执行？\n\n这是典型的**“渐进式降级（Progressive Degradation）”**设计哲学。\n\n- 为什么 `context collapse` 放在 `autocompact` 之前？\n\n因为两者的**破坏性（Destructiveness）****互斥**的。\n\n- 压缩结果如何影响后续 API 请求和 UI 消息？\n\n压缩管线在底层巧妙地分离了“机器看到的”和“人类看到的”视图：\n\n- 这套管线体现了怎样的 Context Engineering 思想？\n\n这套四层管线是 **Context Engineering（上下文工程）** 理论的巅峰教科书，体现了三大核心思想：\n\n**缓存感知（Cache Awareness）是一等公民：** 系统不仅仅是为了省空间，更是为了**保缓存**。Microcompact 中复杂的 `Cache Edits` 路径设计，完全是为了在裁减历史的同时，不打破 API 严格的前缀匹配原则。这意味着在工业级 Agent 设计中，降低 Token 的终极目的是商业可用性。**反遗忘设计（Anti-Amnesia）：** 也是最精妙的一点。当执行完最具破坏性的 Autocompact 后，模型通常会“断片”（忘记当前打开了什么文件、启用了什么技能）。因此，源码在压缩完成后设计了一个“重建阶段”，系统会**自动将最近操作的最多 5 个关键文件（每文件限制 5K Tokens）和活跃的 Skill 指令作为附件重新注入回话流**。这种“压缩后自动恢复工作记忆”的闭环，确保了模型动作的连贯性。**把上下文视为“稀缺预算”进行精确管理：** 上下文窗口不仅是被动堆填的垃圾桶。管线在每轮执行前会精确计算 Token `Warning`、`Error` 和 `Autocompact` 的阈值缓冲。通过“读时投影”和“截断传递（`snipTokensFreed` 传给下一步）”，系统就像内存垃圾回收器（GC）一样，用最低的延迟代价回收最大的思考空间。",
      "普通压缩（如直接从头部截断或全量 LLM 摘要压缩）往往会直接修改或替换本地的消息历史，而 Cache Edits（增量删除缓存）是一种极具前瞻性的黑科技，其核心在于**绝对不修改本地消息序列，而是通过 API 旁路指令让云端动态跳过特定内存块**。\n\n以下是对该机制核心设计逻辑的深度拆解：\n\n**1. 为什么 cached microcompact 不直接修改本地** **messages****？**\n\n这源于大模型 API 的一个核心限制：**“缓存悖论”**。 大模型的 Prompt Cache 机制强依赖于**前缀序列的绝对完全匹配**。在长对话中，如果你为了节省 Context Window 空间，直接在本地 `messages` 数组里删除了中间某段冗长的工具执行结果（`tool_result`），会导致发往云端的消息序列前缀发生断层式改变。云端一旦发现前缀与已缓存的树结构对不上，之前的全局缓存就会瞬间全部失效，导致后续所有的 Token 都必须重新计算，这会让长对话的响应变得极慢且 API 成本飙升。因此，**不修改本地** **messages** **是为了保住极其昂贵的缓存前缀不被破坏**。\n\n**2.** **pendingCacheEdits****、****cache_edits****、****cache_reference** **的各自职责**\n\n这三个机制共同构成了一条与云端缓存通信的“侧信道（Side-channel）”：\n\n**cache_reference****（缓存引用标签）**：它的职责是**作为数据块的唯一标识符（条形码）**。系统在每次向模型发送工具执行结果时，会在底层给这个数据块贴上一个基于 `tool_use_id` 的唯一引用标签，方便后续能够精准定位到它。**cache_edits****（缓存编辑指令）**：它的职责是**向云端下达精准的“读时跳过”命令**。当 Microcompact 判定某些旧工具结果需要清理时，系统会构建一个包含 `{\"type\": \"delete\", \"cache_reference\": \"tool_use_id\"}` 的 API 指令块，告诉云端在处理上下文时，直接将这些被标记的块视为“不可见”或直接跳过。**pendingCacheEdits** **/** **pinCacheEdits****（持久化删除记录）**：它的职责是**保存并重放删除状态**。因为服务端的原始缓存结构并未被真正物理抹除，系统必须在本地把这些已提交的删除指令保存下来（`pinCacheEdits()`），并在后续的每一轮请求中自动附带发送，以确保那些庞大的冗余块在未来的交互中被持续跳过。\n\n**3. 这种设计如何兼顾 prompt cache、resume/debug 可恢复性和请求成本？**\n\n这体现了极其精妙的工程平衡（Harness Engineering）：\n\n**兼顾 Prompt Cache 与 请求成本**：由于本地的消息序列原封不动，发往云端的整体请求前缀依然完美匹配，**Prompt Cache 得以继续 100% 命中**。同时，由于云端在实际注意力计算时跳过了被 `cache_edits` 标记的庞大日志内容，模型实际处理的新 Token 数量大幅下降，从而在**极限压榨 API 成本的同时显著降低了系统延迟**。**兼顾 Resume/Debug 的可恢复性**：因为系统绝对不修改底层的本地 `messages` 数组，所有最原始的工具调用和执行日志在本地会话（Session）文件及 REPL 内存中依然完好无损地保存着。这意味着，如果用户使用 `--resume` 恢复会话，或者需要 Debug 排查 Agent 之前到底看到了哪些源码报错，**所有最原始的颗粒度数据都 100% 可追溯**，没有任何信息被不可逆地抹除。\n\n**4. 它和 System Prompt 动静分离的思想有什么共同点？**\n\n两者在核心设计哲学上高度一致：**都是为了在不可避免的动态变化中，死死保住长文本的“静态缓存前缀”，将大模型的 Prompt Cache 视为第一等公民**。\n\n**System Prompt 动静分离**：通过插入 `SYSTEM_PROMPT_DYNAMIC_BOUNDARY` 分界线，在物理层面将“永不改变的系统规则（静态）”与“频繁变化的会话数据（动态）”强行解耦，向云端保证分界线前的 Token 绝对稳定，从而实现跨用户共享缓存。**Cache Edits 黑科技**：同样是为了保持请求“前缀”的绝对稳定（静态），它将需要删除的旧数据（动态变化）转化为不影响前缀序列的**旁路指令**（侧信道操作），从而完美骗过大模型的缓存失效判定机制。\n\n两者的共同底色是 **Cache 感知的架构设计**，即宁可牺牲代码指令流的直观性（引入复杂的动态注入、分块、或旁路删除记录），也要换取大模型在工业级长周期对话中极致的 API 算力复用率与成本优化。"
    ]
  },
  "tool-system": {
    "title": "Tool 接口和流式编排",
    "ref": "问题组 5.1 / 5.2",
    "answerSource": "notebookAns.md",
    "questions": [
      "Tool 接口中哪些字段构成一个工具的最小契约？请按输入、执行、权限、并发、安全展示归类。",
      "isConcurrencySafe、isReadOnly、checkPermissions 的默认值分别体现了什么安全哲学？",
      "为什么“不确定是否安全”时，Claude Code 更倾向于按不并发、非只读处理？",
      "StreamingToolExecutor 如何在并发和顺序之间取舍？请结合队列和 canExecuteTool 解释。",
      "当用户接入了包含几十上百个外部能力的 MCP 服务器时，如果将所有的工具 Schema 都塞给模型，会立刻撑爆上下文。Claude Code 是如何解决这个问题的？"
    ],
    "answers": [
      "在 Claude Code 源码的 `Tool` 接口中，构成一个工具最小契约的核心字段可以归类如下：\n\n**输入 (Input)**：`inputSchema`（基于 Zod 的强类型输入验证）、`inputJSONSchema`（用于 MCP 工具互操作的 JSON Schema）以及 `validateInput`（工具执行前的业务逻辑验证）。**执行 (Execution)**：`call`（核心执行函数，接收输入和上下文并返回实际的 `ToolResult`）。**权限 (Permission)**：`checkPermissions`（返回针对当前调用的工具特定权限决策，如 allow/ask/deny）。**并发 (Concurrency)**：`isConcurrencySafe`（布尔值或函数，声明该工具在当前输入下能否与其他工具放入同一个批次并行执行）。**安全展示 (Safety/Display)**：安全标识包含 `isReadOnly`（是否只读）、`isDestructive`（是否是不可逆的破坏性操作）、`interruptBehavior`（被打断时的行为）；展示契约包含 `renderToolUseMessage` 和 `renderToolResultMessage`（终端 UI 渲染回调）。",
      "这三个默认值体现了极其老道的**“内部保守，外部兜底”**的安全平衡哲学：\n\n**isConcurrencySafe: false** **和** **isReadOnly: false****（内部保守 / Fail-closed）**：系统在默认情况下把任何新工具都当成“最危险的写入工具”对待。假设它不可并发，是为了防止多个并发写操作导致文件行号偏移冲突；假设它不是只读的，是为了确保它必然会触发后续最严苛的安全审查级别。并发和脏写是工具内部需要自己负责的事，所以默认值给得最保守。**checkPermissions: allow****（外部兜底 / Fail-open）**：这看起来很宽松，但源码的真实用意是：工具内部不需要去重复实现基础的拦截逻辑。因为 Claude Code 外层有一套高达 6 层的纵深防御网络（包含正则拦截、用户设置、Hook 脚本和 AI 审查分类器）。工具默认放行，是将基础的安全拦截职责剥离并交给了全局的系统网来兜底。",
      "这是基于**“Fail-closed（默认失效）”**底线原则的工程实践，主要为了防止大模型幻觉带来的灾难性后果：\n\n**为何倾向于不并发**：在源码的 `StreamingToolExecutor` 并发控制中，如果 `isConcurrencySafe` 抛出异常（比如输入解析失败，无法确定是否安全），系统会直接将其视为不安全并切断并发分区。因为宁可让程序退化成老老实实的串行执行（牺牲一点几十毫秒的 I/O 性能），也绝对不能冒着并发写同一个文件导致代码库完全损毁的风险。**为何倾向于非只读**：如果无法确定一个工具是否只会读取数据，系统就会把它当成“会修改环境”的危险工具。这会导致该工具的调用被送入昂贵的 YOLO 分类器深度审查，甚至直接在终端弹窗要求人类输入 `y/n` 确认。这种设计确保了 AI 任何模棱两可的操作，都会在物理执行层被强行截停，杜绝了“悄悄删库跑路”的可能。",
      "在 Claude Code 中，`StreamingToolExecutor` 是极致压榨 I/O 性能并兼顾执行安全的调度枢纽。其内部的工具调度逻辑极其精妙，具体拆解如下：\n\n**1. 工具如何进入队列** `StreamingToolExecutor` 采取了“边流式接收边执行”的极限并发策略。在模型通过 API 流式返回响应的过程中，系统不会等待整个文本生成完毕。每当底层的解析器识别到一个完整的 `tool_use` JSON 块，就会立即调用 `addTool()` 方法将该工具排入队列。这意味着当大模型还在生成第二个工具调用的参数时，第一个工具已经在本地开始运行了。\n\n**2.** **inputSchema.safeParse** **与** **isConcurrencySafe** **的并发判断机制**\n\n**状态声明**：每个工具在定义时都会通过 `isConcurrencySafe` 属性声明自己是否可以安全地并发执行，默认值极其保守，为 `false`（不可并发）。**输入验证的兜底**：在进入调度前，系统必须解析工具的输入以判断其安全性。此时，如果 `inputSchema.safeParse` 验证失败，或者在调用 `isConcurrencySafe(input)` 时抛出任何异常，系统会触发极致的防御性设计（Fail-closed 原则）。系统会直接默认该工具为**不安全（不可并行）**，宁可将其降级为串行执行从而损失性能，也绝对不冒解析错误带来的并发冲突风险。\n\n**3. 工具分区的核心规则** 工具进入执行器后，并非简单的一拥而上，而是通过 `partitionToolCalls` 进行精确的**分区编排**：\n\n**并行分区**：连续被标记为并发安全（`isConcurrencySafe: true`，如多个 `FileRead`）的工具，会被打包进同一个批次，同时并发执行。**切断与串行**：一旦队列中出现非并发安全的工具（`isConcurrencySafe: false`，如 `FileEdit`），系统会立刻切断当前的分区。非安全工具必须等待前面的并行批次全部完成后，单独串行执行，执行完毕后才能开启下一个分区。**上下文延迟修改**：并发执行的工具若返回了影响全局的 `contextModifier`（如更改了当前目录），这些修改器会被收集起来延迟应用，以防止并发工具相互篡改运行环境。\n\n**4. 安全工具并发与不安全工具阻塞的权衡** 这种设计的核心是在“物理极限的低延迟”与“毁灭性的状态冲突”之间寻找最优解：\n\n**性能压榨**：像读取文件、Grep 搜索等只读工具天然无冲突。通过将其打包并发，如果模型决定一次性读取 10 个文件，总耗时不再是累加，而是仅等于读取最慢那一个文件的时间（例如约 50ms），极大降低了长对话中的 I/O 等待感。**防冲突妥协**：写入类工具必须串行阻塞。因为如果两个 `FileEdit` 工具并发修改同一个文件，极易导致致命的行号偏移、代码覆盖等状态损坏。系统宁可在这里发生线程阻塞，也要保障代码库的绝对安全。\n\n**5. 降低工具错误扩散的防御机制** 面对大模型可能产生的幻觉和级联错误，执行器引入了极强的中断与清理机制：\n\n**Sibling Abort（兄弟中止）**：通过内置的 `siblingAbortController`，在一个并行批次中，如果某个工具（例如长时间运行的 `Bash` 脚本）突然抛出致命错误，控制器会立刻触发中止信号，直接“杀死”当前批次中所有仍在运行的兄弟工具。这防止了无用任务继续消耗算力，但最精妙的是，这一中止被隔离在了当前批次，**不会向上传导中止父级的主查询循环**，从而让 Agent Loop 能够接收到错误并优雅地进行重试。**Streaming Fallback（流式回退的 discard 机制）**：当底层的 API 遭遇网络错误、达到输出上限（`max_output_tokens`），或者触发了流式生成的降级回退时，系统会调用执行器的 `discard()` 方法，瞬间丢弃执行队列中所有处于 `pending` 和正在执行状态的工具。这保证了残缺、被截断或已经偏离轨道的无效工具调用不会被错误地提交到操作系统，防止错误扩散到真实文件系统中。",
      "Claude Code 采用了 **Tool Deferral（工具延迟加载）**策略，这是一种典型的“用计算换取前缀空间”的设计：\n\n* **轻量级存根暴露**：对于非核心或低频工具，初始状态下系统不会将完整 JSON Schema 塞入上下文。它只向模型传递一个极小的存根（Stub），包含工具名称和一句简单的检索提示（Search Hint）。\n* **原生 ToolSearchTool 检索**：当模型在处理任务时认为可能需要某项未展开的外部能力，它必须主动调用内置的 `ToolSearchTool`。此时，系统才会拉取该工具完整的参数 Schema，并作为新的消息追加进去。\n* **双赢的架构收益**：这保证了即使挂载 100 个外部工具，首轮请求的 System Prompt 前缀依然极度瘦身，并保持字节级稳定，利于命中 Prompt Cache。同时，它也降低了模型行为扰动，避免无关工具参数手册干扰当前推理注意力。"
    ]
  },
  "permission-hooks": {
    "title": "权限漏斗与 Hook 不变量",
    "ref": "问题组 6.1 / 6.2",
    "answerSource": "notebookAns.md",
    "questions": [
      "请系统讲解 Claude Code 的权限模型：permission mode、allow/deny/ask、deny 优先、Auto 分类器分别处在什么层次。",
      "为什么 Hook allow 不能绕过 settings.json 里的 deny/ask？这个不变量避免了哪些安全问题？"
    ],
    "answers": [
      "以下是 Claude Code 权限模型的系统性解析，完全基于源码层面的硬核流转逻辑，抛弃抽象比喻：\n\n1. 7 种权限模式（Permission Mode）的精准定位\n\n权限模式是系统在运行时对操作意图进行降级或放行的基准刻度。\n\n- `allow`、`deny`、`ask` 三种行为的确切含义\n\n这三种行为不仅代表最终决策，更是权限评估管线（Evaluation Pipeline）内部流转的信号状态：\n\n- 为什么 `deny` 规则具有最高优先级？\n\n这是基于安全工程中经典的 **“默认失效（Fail-closed）”** 原则。 在 Claude Code 的执行流中，存在一个被严格贯彻的核心安全不变量（Invariant）：**用户的显式拒绝和系统的硬编码拒绝，绝对不可被任何机制绕过**。 如果允许 `allow` 覆盖 `deny`，系统将面临灾难性的越权漏洞（Escalation Risk）：例如用户安装了第三方的 MCP 服务器插件，且该插件包含一个恶意 Hook 强制对所有操作返回 `allow`。如果优先级倒置，该第三方插件就能轻易绕过系统 `settings.json` 中配置的防删库跑路规则（如 `deny: [\"Bash(rm -rf)\"]`）。因此，源码（如 `resolveHookPermissionDecision` 函数）明确规定：即便 Hook 层或分类器返回 `allow`，只要匹配到任何一处 `deny` 规则，权限直接熔断，坚决阻断越权。\n\n4. Path Validation、Bash Permission 与 Tool-specific Permission 的组合逻辑\n\n权限并非单纯正则匹配，而是一条由轻到重、由微观到宏观的严格验证漏斗：\n\n- Auto 模式下两阶段 YOLO Classifier 的作用与风险控制\n\n为了在全自动无人工干预的情况下兼顾 API 成本与运行安全，`auto` 模式舍弃了单一模型，转而引入了双层异步并发监控的 AI YOLO（You Only Look Once）分类器：\n\n**Fast 阶段（第一层快速分类）**：调用更小、速度极快的小模型，仅对当前操作指令本身进行瞬时扫描。只要其返回 `confidence: high` 且判定无风险，请求立刻放行。这解决了 89% 常见操作（如常规查询或标准日志生成）的延迟堵塞问题。**Thinking 阶段（第二层深度推理）**：当第一阶段模型给出不确定的置信度时，请求向上抛至运算深度更广的大模型。大模型将结合整个 Agent 的上下文历史、前置操作序列以及工具输入，开展完整的安全性逻辑研判。如果此阶段仍无法确信，系统直接降级（Fallback）弹窗，转由人类做最终仲裁。**精准风控与防死循环（Circuit Breaker）**：大模型的一个已知缺陷在于“不撞南墙不回头”，如果某个工具被系统连续驳回，模型可能会陷入“尝试 -> 被拒 -> 换种写法尝试 -> 又被拒”的无限昂贵 Token 循环消耗中。为此，分类器内部维护了“拒绝追踪状态（Persist Denial State）”。当系统监测到分类器连续对 Agent 产生 3 次拒绝，或者会话总计触发了 20 次拒绝，熔断器立刻跳闸（Circuit Broken），强行剥离分类器的自动审批权，转交人类处置，或者在无头模式下抛出 `AbortError` 终止整个子 Agent 的运行。",
      "Claude Code 的 Hooks 系统是整个 Harness 架构中最强大的生命周期可扩展系统，它允许用户在 Agent 的运行流程中“见缝插针”地注入自定义逻辑，且提供了高达 26 种事件类型和 4 种执行器类型。\n\n以下针对你的问题进行深度解析：\n\n1. 四类 Hook 的适用场景\n\nHook 系统根据“成本”与“智能度”的平衡，被划分为四种类型，各自适用于完全不同的业务场景：\n\n- 关键事件在生命周期中的位置\n\nHook 事件类似于浏览器的捕获/冒泡模型，以下事件在工具执行管道（Tool Execution Pipeline）中占据了精准的位置：\n\n- 为什么 Hook allow 不能绕过 settings.json 的 deny/ask\n\n这源于 Claude Code 对**安全不变量（Security Invariant）**和“防御性编程”的极致追求：\n\n如果 Hook 返回的 `allow` 能够绕过系统配置的 `deny`，系统将面临灾难性的越权风险。假设用户安装了一个恶意的第三方 MCP 服务器，该服务器携带了一个 `PreToolUse` Hook，无论什么操作都无脑返回 `allow`。如果允许它绕过 `deny` 规则，那么这个第三方插件就获得了超越用户全局安全策略的最高权限，能随意执行 `rm -rf` 等被用户明令禁止的高危指令。因此，系统的配置必须作为“最后的物理隔离墙”，第三方或定制逻辑永远不能越权。\n\n4. `resolveHookPermissionDecision` 如何保证安全不变量\n\n在源码的权限处理逻辑中，`resolveHookPermissionDecision` 是判决 Hook 和系统规则的核心枢纽。它的代码逻辑强制规定：**deny** **>** **settings rules** **>** **hook allow**。\n\n当 `PreToolUse` Hook 的判定结果 `hookPermissionResult.behavior` 为 `'allow'` 时，源码不仅没有直接 `return` 批准，反而会将这个结果视作一种“建议”，继续让程序向下流转去匹配 `settings.json` 中的 `deny` 和 `ask` 规则。只有当所有系统层面的硬性规则（含 `CLAUDE.md` 的强制拒绝和 `auto` 模式的安全检测）都没有将其拦截时，Hook 的 `allow` 才真正起效。只要命中了系统配置中的任何一条 `deny` 策略，该工具调用依然会被直接阻断。\n\n5. 企业的审计与审批场景如何使用 Hooks（且不变成后门）\n\n企业可以将 Hooks 作为强大的合规利器，具体实现方式如下：\n\n**自动化静默审计（100%留痕）**： 企业可以在 `PostToolUse` 或 `SessionStart` 事件上挂载 `HTTP Hook` 或 `Command Hook`。为了不阻塞 Agent 正常的开发工作流，配置参数中必须设定 `\"async\": true`。这样每次工具执行完成后，日志会以非阻塞（fire-and-forget）的形式异步 POST 到企业的 Datadog 或审计网关中，实现了透明的动作追踪。**企业合规准入与拦截**： 企业可通过 `PreToolUse` 挂载 `Command Hook`，在每次敏感操作前执行类似 `guard-rules.js` 的脚本（例如扫描代码是否包含 AWS Key 或是否使用了 `sudo`）。一旦脚本检测到违规，向标准输出返回 `{\"decision\": \"block\", \"reason\": \"Secret detected\"}`，便能在执行前将危险切断。**防止 Hooks 变成提权后门**： 结合前述的第 3 和第 4 点，企业的安全管理员通过 MDM 设置下发包含底线的 `/managed/managed-settings.json`，在其中将最核心的风险操作（如 `git push --force`，`rm -rf`，修改 `.env` 密钥）写死在 `deny` 数组中。在这一架构下，即便是业务团队自己编写的审批 Hook 出现严重漏洞，对高危操作返回了 `allow`，底层硬编码的 `deny` 规则也会将其拦截兜底，绝对杜绝了利用 Hook 作为后门绕过系统安全的可能。"
    ]
  },
  "bash-fileedit": {
    "title": "Bash 与 FileEdit 的具体防线",
    "ref": "问题组 7.1 / 7.2",
    "answerSource": "notebookAns.md",
    "questions": [
      "请拆解 BashTool 的命令注入检查、复合命令子命令检查、deny/ask 优先级，说明这些设计如何避免规则被绕过。",
      "请拆解 FileEditTool 的读后写、文件修改时间检查、old_string 唯一匹配检查，说明它们分别防哪类错误。"
    ],
    "answers": [
      "结合 Claude Code 源码的纵深防御架构，BashTool 的安全防线设计如下：\n\n**命令注入检查的触发时机**： 在 `BashTool` 接收到大模型生成的 Shell 字符串后，实际抛给系统底层执行**之前**触发。系统会将其送入 `bashSecurity.ts` 构筑的 20+ 道安全检查流水线，拦截超过 25 种容易导致越权或命令逃逸的模式（例如 `$()` 命令替换、`<()` 进程替换、Unicode 隐藏字符、引号反同步，以及 `zmodload` 等 Zsh 专属危险特性）。**AST parse 成功与 legacy regex 检查的关系**： 这是“结构化解析”对“弱正则匹配”的降维打击。遗留的正则规则（如 `Bash(npm:*)`）如果直接作用于完整字符串，极易被语法欺骗。系统利用 `tree-sitter` 将命令解析为 AST（抽象语法树），从中安全地提取出最底层的 `SimpleCommand`。AST 解析成功后，系统**只针对提取出的纯净子命令**去应用遗留的正则表达式和安全验证，从而彻底堵死通过字符串拼接绕过正则的漏洞。**为什么复合命令要拆成 subcommands 再检查**： 为了防止“夹带私货”。如果不对复合命令进行拆分，一条长命令 `cd /path && python3 evil.py` 极有可能因为前缀是 `cd` 而错误命中了 `Bash(cd:*)` 的放行规则。将其拆分为独立的 subcommands 后，系统能精准拦截后半段的危险脚本。此外，限制子命令拆分上限为 50，也是为了防止由于过度嵌套导致的事件循环饥饿与 ReDoS 攻击。**为什么 subcommand deny 必须早于 full-command ask**： 这是为了防范“人类点击疲劳”造成的盲目授权。遵循“一票否决（Fail-closed）”原则，如果一个长串复合命令中藏着一条致命的 `rm -rf /`，即使它的外层命令触发了 `ask` 弹窗，`deny` 也必须强制抢占最高优先级并阻断整体执行。否则，习惯性点击“Yes”的用户就会无意间放行恶意代码。**sandbox auto allow 为什么仍然要尊重显式 deny/ask 规则**： 因为沙盒（Sandbox）和权限规则防范的“危险维度”完全不同。沙盒是操作系统级别的物理隔离（防止恶意修改系统文件或访问未知 IP），它是兜底的“安全网”而不是“免死金牌”。即使一个命令（如 `git push --force` 或 `npm publish`）在沙盒网络内被认为是“物理安全”的，它依然可能严重破坏团队的代码协作与业务规范。因此，基于业务意图的显式 `deny/ask` 规则拥有最高逻辑优先级，沙盒在自动放行时必须向下将控制权交给 `BashTool`，由其执行具体的命令级规则检查。",
      "FileEditTool 的编辑安全逻辑是 Claude Code 防御大模型代码幻觉的最后一道物理防线。其核心设计通过严格的状态校验和字符串匹配算法，确保模型“只能修改它真正看清且唯一确定的内容”。以下是其安全逻辑的深度拆解：\n\n**1. 为什么写文件前必须确认文件已经被完整读取？** 大模型存在严重的“记忆幻觉”，极易凭借预训练数据凭空想象代码库的结构和内容。为了从物理层面掐断这种幻觉，系统强制规定“不能编辑未读过的文件”。模型必须先使用 `FileReadTool` 读取文件，系统会通过 `readFileState` 缓存来跟踪并记录该读取状态。如果模型试图直接编辑未读文件，系统会无情拒绝并提示其先读取，这迫使模型在编辑前必须基于文件的“当前真实状态”而非“凭记忆的想象状态”进行操作。\n\n**2.** **readFileState****、时间戳与内容比较分别防什么问题？**\n\n**readFileState**：防“盲改”。它充当一个访问令牌，确保模型在当前会话中真实且有效地观测过该文件。**时间戳（缓存过期与释放）**：防“状态滞后”。在多 Agent 并发和复杂会话中，文件状态缓存（`readFileState`）会在特定的生命周期被清理或释放，强制要求模型重新读取，防止模型基于陈旧的、已被其他进程修改的历史快照进行危险操作。**内容比较（****old_string** **匹配）**：防“上下文偏移”。大模型生成的 `old_string` 本质上是一个内容校验和（Checksum），它强制模型在替换前，必须在文件中找到完全一致的代码块，从而防止因其他人修改文件或模型对行号理解错位而导致的“改错位置”灾难。\n\n**3.** **old_string** **找不到、多处匹配、****replace_all** **的处理逻辑**\n\n**多处匹配**：`old_string` 必须在文件中“唯一匹配”。如果找到多处相同的代码块，编辑会直接失败并报错，要求模型提供更多上下文（如包含更多的上下游代码行）以精确定位。**找不到**：如果精确匹配失败，系统不会立刻放弃，而是会进入“引号规范化（Quote Normalization）”的三阶段查找算法进行降级匹配。**replace_all** **与匿名函数防御**：当启用 `replaceAll` 时，系统会将所有匹配项执行替换。这里的核心防御细节是：替换过程强制使用匿名函数 `() => replace` 进行替换，而不是直接传递字符串。这防止了替换字符串中包含的 `$1`、`$&` 等特殊模式被 JavaScript 引擎误解释为正则表达式的捕获组注入。此外，若执行删除操作（`newString` 为空）且 `oldString` 不以换行结尾，当原文中该字符串后紧跟换行时，系统会智能地连同换行符一并删除，防止留下破坏代码结构的空行。\n\n**4. 智能引号或 quote normalization 为什么会影响真实编辑？** 模型在生成要替换的 `old_string` 时，常会产生“排版幻觉”，将代码里标准的直引号（`\"` 或 `'`）错误输出成弯曲的智能引号（`“`、`”`、`‘`、`’`）。如果不做处理，直接使用严格的精确匹配会导致寻找失败。 为此，系统引入了引号规范化逻辑：把搜索词和文件内容全部转换为直引号后再搜。最关键的是，定位到匹配位置后，系统会**提取并返回文件里的“原始文本”（保留原始的直引号风格）**作为真正的替换目标。这样既宽容了模型不可控的排版幻觉，又绝对保证了不会把非法的智能引号注入到源码中引发语法报错。\n\n**5. 这套防线如何减少模型幻觉造成的错误编辑？** 这套防线将大模型的编辑行为从“随性创作”约束为了“严格的外科手术”：\n\n**强制观测**（`readFileState`）断绝了凭空捏造代码、幻觉改写的可能。**唯一性约束**（`old_string` 唯一匹配）防止了盲目替换导致误伤无辜代码行。**智能容错与防御性替换**（引号规范化、匿名函数防止注入）在包容 LLM 固有生成缺陷（如智能引号错误、特殊正则字符误用）的同时，守住了目标文件的绝对语法正确性。"
    ]
  },
  "memory-system": {
    "title": "Memory 召回和企业同步",
    "ref": "问题组 8.1 / 8.2",
    "answerSource": "notebookAns.md",
    "questions": [
      "请解释 Claude Code Memory 的主动召回链路：扫描哪些文件、为什么只读 frontmatter、如何过滤 alreadySurfaced、如何选出相关记忆。",
      "Team Memory 上传前扫描 secret 的主要风险模型是什么？",
      "为什么发现 secret 后应跳过整个文件，而不是只删除命中的那一行后继续上传？",
      "日志只记录第一个 secret match 的规则和标签，这样做有什么安全意义？",
      "长期运行的 Agent 如何对抗记忆系统的“熵增”和碎片化？请解释 AutoDream 引擎的作用。"
    ],
    "answers": [
      "Claude Code 的主动召回（Active Recall）机制是一套为了在长对话中动态唤醒相关记忆，同时严格控制 API 成本与上下文膨胀的精密流水线。对于源码学习者而言，这条链路的每一个设计都充满了工程上的取舍与智慧。以下是该链路核心节点的详细拆解：\n\n**1. 扫描哪些** **.md** **文件，为什么排除** **MEMORY.md****？**\n\n**扫描范围**：每当用户输入新消息时，系统会递归扫描自动记忆目录（`getAutoMemPath()`，包括其子目录 `team/`）下的所有 `.md` 文件。如果用户在消息中 `@mention` 了某个特定的 Agent，则会切换到该 Agent 的专属记忆目录进行扫描。**排除** **MEMORY.md** **的原因**：`MEMORY.md` 作为全局记忆索引，已经被另一条静态注入链路（路径 B）直接包裹在 `<system-reminder>` 中作为第一条 User Message 发送给了模型。如果在主动召回阶段再次扫描并注入它，会导致昂贵的 Token 浪费和上下文冗余。因此，底层的 `scanMemoryFiles()` 会通过 basename 强行过滤掉它。\n\n**2. 为什么召回阶段只读 Frontmatter 和有限行数（30 行）？**\n\n这是极致的 **I/O 与性能优化**。由于记忆目录中可能存在大量文件，如果在每轮对话前都完整读取所有文件内容来进行语义匹配，会导致巨大的延迟。源码中硬编码了 `FRONTMATTER_MAX_LINES = 30`，系统仅读取文件头部的前 30 行来解析 Frontmatter（提取 `type` 和 `description` 字段）。这意味着主动召回的精准度高度依赖于 Frontmatter 中 `description` 的摘要质量。这种“只看摘要”的设计让每次对话前的 Prefetch（预取）动作可以在几毫秒内完成。\n\n**3. 排序、限制与过滤机制分别解决什么问题？**\n\n**按** **mtime** **排序**：系统通过读取文件的最后修改时间（mtimeMs）进行倒序排列，优先处理最新被修改或写入的记忆，确保模型优先接触到最新的项目状态。**最大文件数限制（****MAX_MEMORY_FILES = 200****）**：防止项目庞大时扫描过多文件导致 I/O 阻塞或构造候选清单时消耗过多内存与 Token。**alreadySurfaced** **过滤机制**：这是一个跨 Turn（轮次）累积的 `Set<string>` 集合。如果一份记忆文件在之前的轮次中已经被选中并注入过上下文，它就会被加入该集合，在后续轮次中被无情剔除。这完美解决了**Token 浪费问题**，确保同一份记忆在一个长会话中绝对不会被重复注入。\n\n**4.** **selectRelevantMemories** **(Sonnet 裁判) 在链路中的角色**\n\n在这个环节，系统不会使用昂贵的主模型（如 Opus）来进行全文匹配，而是构建一个轻量级的候选记忆清单（Manifest，仅包含文件名和摘要），并连同用户的最新问题，作为 Side-Query 发送给更快、更便宜的小模型（Claude Sonnet）。**裁判职责**：Sonnet 在这里扮演“裁判”和“排序器”的角色，它负责从清单中精选出最多 5 个最相关的文件，同时会智能过滤掉用户最近刚用过的工具（`recentTools`）的 API 文档记忆（因为这些已经存在于上下文中）。这种设计用极低的 API 成本实现了比传统向量数据库更精准的“语义级别的记忆唤醒”。\n\n**5. Memory Freshness（新鲜度算法）如何抑制幻觉？**\n\n大模型极容易将“过期的代码状态”（比如几天前的行号或已删除的函数）当成当前事实，从而产生灾难性的幻觉。为了抑制这种幻觉，系统在读取被 Sonnet 选中的完整文件内容后，会基于 `mtimeMs` 计算天数。如果距今超过 1 天（≥2），系统就会判定为过期记忆（Staleness）。**强制警告注入**：对于过期记忆，源码会强制在正文头部硬拼接一段威慑性警告（Freshness Note），明确告诉模型：“这份记忆已经 X 天了，它只是历史快照。在断言事实前，必须根据当前代码进行验证。” 结合 System Prompt 中要求模型“优先使用 Grep 验证”的硬规则，从工程底层强行扭转了 AI 过度信任陈旧记忆的倾向。",
      "Team Memory 上传前扫描 secret 的核心风险模型是“私有敏感凭证的无意共享与云端扩散”。由于 Team Memory 机制的设计初衷是让同一个 GitHub 仓库的所有协作者通过 Anthropic API 跨机器双向同步和共享团队级的知识底库，它在本质上是一个对整个团队开放的云端共享空间。如果 Agent 在自动提取记忆或处理日志时，误将开发者本地的 AWS Keys、GitHub PATs、Slack Tokens、PEM 私钥等私有凭证写入了 Team Memory 文件，这些机密数据就会随着后台定期的 Push 操作直接同步到远端服务器，进而分发给所有具备该仓库访问权限的其他成员。为了防止这种灾难性的横向凭证泄露，系统内置了 40+ 条强力的 gitleaks 规则，强制在写入（FileWriteTool/FileEditTool）或 Push 动作发生之前进行拦截阻断。",
      "发现 secret 后选择彻底跳过整个文件而不是仅删除命中行，主要基于“Fail-closed（默认失效与保守兜底）”的安全架构哲学以及上下文完整性考量。首先，许多高危密钥（如 PEM 格式的私钥或经过复杂分行拼接的长 Token）通常是跨越多行存在的，简单的单行删除极易造成密钥结构的残断遗留或周边敏感上下文信息的暴露，无法确保安全威胁被完全清除。其次，系统的设计原则倾向于“显式阻断”而非“静默篡改”。如果系统在后台悄悄删除命中行后继续上传，会导致大模型的本地认知状态与云端实际保存的记忆产生物理割裂与不可预期的不一致。因此，系统直接拦截并抛出严重的错误提示（\"Content contains potential secrets and cannot be written to team memory...\"），能够强制切断该写入任务，倒逼开发者或 Agent 从源头上排查并剥离这些机密信息。",
      "日志中仅记录第一个匹配的规则与标签，具有关键的“数据脱敏（PII 隔离）”与防止二次泄露的安全意义。首先，如果在日志系统中记录命中特征的具体明文内容，相当于将原本被成功拦截的私钥或 Token 以明文形式直接写进了本地系统日志，甚至可能随着遥测系统（如 1P Event Logging 或 Datadog）被错误地传输到外部服务器，这会引发比上传记忆更严重的“二次泄露”灾难。仅记录规则标签（如指示命中了“AWS Key”或“Slack Token”的拦截策略）既能为后续的诊断审计提供充分的排障线索，又能在物理层面上彻底剥离个人敏感身份信息（PII）的外流可能。此外，找到首个违规 Secret 即宣告文件不安全并触发“短路求值（Short-circuiting）”停止扫描，能极大避免针对庞大文本内容强行跑满全部 40+ 条复杂正则表达式所带来的无谓性能惩罚与计算开销。",
      "如果记忆只增不减，系统会越来越“自信地错”。除了每轮结束时的即时提取（extractMemories），Claude Code 还设计了跨会话的离线巩固引擎：**AutoDream（自我进化机制）**。\n\n* **触发条件极高**：系统不会频繁做全局整理。只有当距离上次整理超过 24 小时，并且累积了 5 个新的 Session 后，系统才会在后台申请 PID 互斥锁，拉起 AutoDream 子 Agent。\n* **类生物睡眠的去噪过程**：它遍历庞大的旧日志和转录数据，不仅负责把相对时间（如“昨天”）转为绝对日期，更负责合并重复记忆、解决前后矛盾（如两个文件有冲突时修复错误的一个），并删除被证伪的过时记忆。\n* **最核心的 Token 预算强控**：AutoDream 被下达了死命令：必须强制将全局索引文件 `MEMORY.md` 裁剪在 200 行或 25KB 以内。这是对抗长期运行导致知识熵增、防止索引本身撑爆上下文的最核心物理防线。"
    ]
  },
  "multi-agent": {
    "title": "多 Agent、Fork 与 Coordinator",
    "ref": "问题组 9.1",
    "answerSource": "notebookAns.md",
    "questions": [
      "请对比普通 AgentTool、Fork Subagent、Coordinator 三种协作方式：上下文如何传递、权限如何处理、什么时候不应该委派理解任务。"
    ],
    "answers": [
      "在面对复杂的重型开发任务时，Claude Code 并没有选择简单的单线程执行，而是通过普通 AgentTool、Fork、Coordinator 三种不同的协作模式构建了一个多 Agent 的“分布式军团”,。以下是面向源码学习者对这三种协作方式核心机制的深度拆解：\n\n1. 上下文如何传递或隔离\n\n- 工具池、模型与权限模式的继承\n\n- Fork 为什么强调 byte-identical prompt prefix 和 cache reuse？\n\n这属于极致压榨 API 成本的 Context Engineering 策略。 在长对话中，生成 System Prompt 非常昂贵。如果让并行派生的多个 Fork 重新去生成 System Prompt，极易触发某些 Feature Flag 的状态重算，导致字节发生微小改变，进而打破 API 严格的前缀匹配原则。 为此，Fork 模式在内存中**字节级复制（byte-identical）父进程已渲染完毕的 System Prompt 和对话前缀**，仅在末尾追加针对 child 的差异化指令。这使得 5 个并行启动的 Fork 任务能**完美复用父进程的 Prompt Cache**，实现“并发 5 个 Agent 探索，API 费用约等于 1 个”的惊人成本优化,,。\n\n4. `permissionMode: bubble` 解决什么交互问题\n\n在 Swarm/Team 这种多 Agent 持久化协作模式中，如果 4 个 Worker 都在后台尝试执行危险操作（如修改文件或运行命令），传统的交互会在它们各自的隐藏进程或终端分屏中弹出 4 个独立的审批对话框。这会导致用户为了授权而不得不在多个窗口之间疲于奔命。 配置 `bubble`（冒泡）模式后，底层的 `Leader Permission Bridge` 拦截器会将所有 Worker 的权限请求打包，通过文件系统邮箱统一“冒泡”到主控（Leader）进程的 UI 上,。**用户只需盯着 Leader 的主终端界面集中审批**，授权结果会自动沿邮箱回调给 Worker 恢复执行，完美解决了异步 Agent 并发的交互体验问题,,。\n\n5. Coordinator 为什么要求“不要委派理解任务”？\n\nCoordinator（协调器）在源码中被定义为没有底层读写工具（如没有 Bash、Edit）的纯编排者。 因为它的 Worker 都是从完全空白的上下文（零上下文）启动的，**Worker 根本不知道 Coordinator 之前阅读了什么文件、经历了哪些 debug 过程**。 如果 Coordinator 发送一个模糊指令：“*根据你的发现，修复 auth bug*”（这是源码明确指出的 Anti-pattern 坏模式），Worker 会因为缺乏上下文而像无头苍蝇一样乱撞,。 因此，系统提示词对 Coordinator 下达了铁律：**“永远不要委派理解（Never delegate understanding）”**。Coordinator 必须负责汇总自身的研究结果，将意图拆解为极为明确的指令。 **Worker 的任务信息应该包含**：精确的文件路径、代码行号和变更逻辑（例如：“*修复 src/auth/validate.ts 第 42 行的空指针，因为 sessions 过期时 user 字段为 undefined*”）,。只有这样，零上下文的 Worker 才能精准且高效地执行变更。"
    ]
  },
  "remote-bridge": {
    "title": "Remote/Bridge 与 Worktree",
    "ref": "问题组 10.1",
    "answerSource": "notebookAns.md",
    "questions": [
      "请解释 Remote/Bridge 架构中的 FlushGate、RemoteSessionManager 权限桥接、EnterWorktreeTool 物理隔离分别解决什么分布式协作问题。"
    ],
    "answers": [
      "在 Claude Code 的底层架构中，Remote/Bridge 系统是为了将 AI Agent 从“只能跑在本地终端的小工具”升级为“跨终端、跨环境的分布式 Agent 平台”而设计的核心基建。对于初学者来说，可以将其理解为一套“执行在远端，控制在本地”的远程桌面协议。\n\n以下是对这三大关键设计的详细拆解：\n\n1. FlushGate：如何防止历史消息 flush 和实时消息交错？\n\n**设计原理：** FlushGate 本质上是一个防止消息竞态和乱序的“状态机阀门”。在跨设备同步或断网重连时，客户端需要将本地积压的历史消息同步（flush）到远端服务端。如果此时用户手快，在终端输入了新消息，新消息和正在同步的历史消息一起发向服务端，就会造成严重的对话乱序。\n\n**解决方案：**\n\n- RemoteSessionManager：如何处理各种请求协议？\n\n`RemoteSessionManager` 是云端 Agent 和本地界面之间的双向通信中枢。\n\n- EnterWorktreeTool：如何创建隔离并切换 cwd？\n\n**设计原理：** 当你在同一个代码库里同时拉起 3 个 Agent（一个重构、一个写测试、一个排查 Bug）时，如果它们都在同一个目录下改文件，代码瞬间就会因为相互覆盖而崩溃。\n\n**解决方案：**\n\n- 为什么切换 worktree 后要清理 system prompt section 和 memory cache？\n\n这体现了典型的 Context Engineering（上下文工程）底线：**保证大模型世界观与物理现实的一致性。** 在 Claude Code 的缓存中，当前分支的 Git 状态、项目根目录的 `CLAUDE.md` 规则、以及通过 Memory 系统扫描出的私有经验，都是基于原先的 `cwd` 目录静态计算并被 `systemPromptSection` 冻结在内存里的。 当 `EnterWorktreeTool` 瞬间把进程转移到了另一个物理目录（甚至可能切换了 Git 分支）后，环境已经彻底变了。如果不强制清理这些缓存，Agent 就会带着“旧分支的规则和记忆”在“新工作区”里乱改代码。清空它们是为了迫使系统根据新的 `cwd` 重新读取环境上下文。\n\n5. 这些设计分别解决了什么宏观场景问题？\n\n**FlushGate（解决跨终端问题）**：解决了一个用户在 claude.ai 网页端、CLI 终端和 VS Code 插件之间来回无缝切换时，保证历史聊天记录加载绝对同步，不丢消息不乱序。**RemoteSessionManager（解决跨机器问题）**：实现了“计算与展示解耦”。它让高消耗的 Agent 运行在拥有大算力的企业云端容器里，而高频的 UI 交互和安全审批则安全地推送到开发者本地的轻量级机器上。**EnterWorktreeTool（解决多任务协作问题）**：打破了“单兵作战”的物理限制。它通过低成本的 Git Worktree 隔离，允许最多 32 个会话并发修改同一个代码仓库而互相绝对无脏写冲突，为“分布式 Agent 军团”扫清了物理障碍。"
    ]
  },
  "ui-mcp-skills": {
    "title": "终端 UI、MCP 与 Skills",
    "ref": "问题组 11.1",
    "answerSource": "notebookAns.md",
    "questions": [
      "Ink 终端渲染为什么要批量写入并使用同步更新标记？",
      "MCP 的 stdio、sse、http、ws、sdk 传输适合连接哪些外部能力？",
      "Skill 的 context fork 与普通 prompt skill 在执行隔离上有什么区别？"
    ],
    "answers": [
      "Ink 终端渲染之所以要将 diff 合并并使用 BSU/ESU 同步写入，核心是为了解决命令行应用常见的“画面闪烁”和“渲染撕裂”问题。如果逐行或逐个组件实时打印，终端会呈现出绘制的中间态，导致肉眼可见的闪烁。通过使用双缓冲（Double Buffering）机制，系统在内存中维护当前帧和下一帧，并利用 lodash 节流将刷新率严格锁定在 16ms（60fps）。当计算出实际需要更新的区域后，系统会将完整的渲染指令包裹在 BSU（Begin Synchronized Update）和 ESU 之间发送。这能确保支持该协议的终端硬件在收到完整的结束指令前，绝对不往屏幕上渲染任何半成品的中间状态，从而实现类似 GUI 桌面应用一样丝滑的原子级、零闪烁更新。",
      "MCP 的多协议设计是为了适配不同量级和物理位置的外部集成需求：\n\n**stdio****（标准输入输出）**：适合连接运行在本地环境的轻量级工具（如本地文件系统操作、Git 管理等）。由于它需要拉起真实的本地子进程，为了避免进程生成时的资源争用，底层调度器会将其限制为低并发执行（例如每批次并发 3 个）。**sse****、****http** **和** **ws****（网络协议）**：适合连接远程云端服务或外部 SaaS 平台（如对接 Slack 消息、Linear 任务追踪或远程数据库）。由于纯网络通讯开销小，它们支持高并发执行（如每批次并发 20 个），且 `ws` 能够很好地提供 TLS 加密和代理穿透支持。**InProcess** **和** **sdk****（内存与控制面）**：`InProcess` 专为极其消耗资源的重量级本地工具设计，例如为了避免每次都拉起高达 325MB 内存的 Chrome 浏览器子进程，直接让其作为模块在 Claude Code 主进程内存中运行。`SdkControl` 则专用于作为 SDK 守护进程底层通信的控制面板。",
      "普通的 prompt skill 与 `context: fork` 模式的根本区别在于**上下文历史是否物理隔离**：\n\n**普通 Skill**：直接在主对话的 Agent Loop 中执行，它在工作流中读取的每一个文件内容、运行的每一行测试报错都会直接追加到主对话的上下文中。这不仅会让对话显得极其嘈杂，还会迅速吞噬宝贵的 Token 预算。**context: fork** **模式**：执行时会在后台隐式分叉（Fork）出一个独立的子 Agent。它精妙地继承了父 Agent 已经生成好的 System Prompt 前缀（完美共享 Prompt Cache 从而大幅降低 API 成本），但在执行期间拥有自己完全独立的消息历史和 Token 预算。当该 Skill 执行结束后，所有冗长的中间过程都会被销毁，主对话仅会收到一条极其简练的结果文本，从而实现“上下文不被污染”。**何时使用 fork**：当你需要执行那些**包含多步复杂操作、极易产生巨量日志输出的工作流**时（例如：运行全量单元测试、执行前端构建与部署、对整个模块进行深度的架构审查），必须使用 `fork`。这能有效保护你的主对话免受中间状态的噪音干扰，是 Context Engineering（上下文工程）对抗大模型“失忆”的最佳实践。"
    ]
  },
  "settings-reflection": {
    "title": "设置、遥测与架构反思",
    "ref": "问题组 12.1 / 12.2",
    "answerSource": "notebookAns.md",
    "questions": [
      "Claude Code settings 的宽进严出、passthrough、drop-in managed settings 分别面向什么兼容性或企业管理问题？",
      "managed-settings.json 与 managed-settings.d/*.json drop-in 模式适合什么企业管理场景？",
      "源码中遥测初始化、feature flags、企业记忆同步这些后台能力，和主 Agent Loop 是什么关系？",
      "如果用 6 周学习路线复现一个 Mini Harness，应该按什么顺序练习？"
    ],
    "answers": [
      "Claude Code 的设置文件由用户手动编辑，且需要跨版本持久化。采用“宽进严出”（Postel's Law）原则和 Zod 的 `.passthrough()` 方法，主要是为了解决**跨版本向下兼容性和人工配置的容错问题**。在真实的开发场景中，配置文件极易包含拼写错误、过时的旧字段或来自新版本的实验性未知配置。如果使用严格校验，任何不认识的字段都会导致系统崩溃；而使用 `.passthrough()`，系统会静默忽略并保留这些未知字段而不报错。这确保了无论用户如何升级或降级 Claude Code 版本，旧的设置文件都不会被新版本拒绝或意外洗掉数据，保证了配置的无缝跨版本持久化。",
      "这种 Drop-in 目录模式专门为了解决**大型企业中多部门协同进行策略管控（MDM）时的配置冲突问题**。在企业环境中，配置的不同维度往往归属不同团队：安全团队需要强制下发特定的命令拦截规则（如 `20-security.json`），平台和运维团队负责管理合法的 MCP 插件白名单（如 `10-otel.json`），合规团队则需要配置数据保留策略（如 `30-compliance.json`）。如果所有配置都写在单一的 `managed-settings.json` 中，不同团队在下发策略时极易发生覆写和合并冲突。采用类似 Linux `systemd` 的 `.d` 目录结构，各团队可以独立分发自己的配置片段，系统在启动时自动按字母顺序加载并合并，实现了极高内聚的解耦管理。",
      "这些组件是包裹在主 Agent Loop（`queryLoop`）外围的核心 Harness（工程线束），它们与主循环的关系是**解耦、非阻塞与后台旁路辅助**：\n\n**Analytics Sinks（遥测模块）**：属于启动阶段被调度的重型副作用服务。为了不拖慢 Agent Loop 的启动和响应，如 OpenTelemetry、gRPC 等分析模块采用延迟加载策略，并在后台通过 `BatchLogRecordProcessor` 每 5 秒或凑齐 200 条批量异步发送数据，确保遥测与核心对话循环的性能解耦。**Team Memory Watcher（团队记忆同步）**：它是完全独立于 Agent Loop 的后台守护进程。在启动阶段，它通过 `fs.watch` 递归监听 `team/` 目录。当主循环调用工具修改了文件，Watcher 会在后台基于 2 秒防抖机制（Debounce）自动将增量 push 到远端，完全不阻塞主循环的执行。**Session File Access Hooks（内部文件访问拦截器）**：它们是在主循环的工具执行管道（Tool Execution Pipeline）中被调用的，但采用了极致优化的**快速路径（Fast-path）**。因为它们是内部回调，所以跳过了常规外部 Hook 的 JSON 解析、`abortSignal` 处理等开销，将单次执行延迟从 6.01µs 压缩到了极低的 ~1.8µs，让 Agent Loop 能够在几乎“零感知”的高性能状态下完成细粒度的文件访问记录。",
      "基于 Claude Code 源码总结和实战指南，这里为您提供一份从入门到进阶的 6 周 Mini Harness 实战学习路线。\n\n6 周 Mini Harness 实战学习路线\n\n**第 1 周：基础架构与核心循环 (Agent Loop & Foundations)**\n\n**明确主题**：建立整体骨架认知，跑通从用户输入到大模型流式输出、工具回调的核心循环。**源码模块**：`src/main.tsx` (CLI启动编排)、`src/query.ts` (查询管道)、`src/QueryEngine.ts` (LLM 引擎与循环)。**可实现练习**：在 `src/commands/` 下添加一个 `/hello` 斜杠命令，调用并输出当前时间，理解命令注册与交互执行的完整链路。\n\n**第 2 周：工具系统与安全防线 (Tools & Permissions)**\n\n**明确主题**：理解 Agent 如何安全地与操作系统交互，掌握 Fail-closed 原则与 6 层权限漏斗。**源码模块**：`src/Tool.ts` (基础类型与契约)、`src/tools/BashTool/` (命令沙盒执行)、`src/utils/permissions/` (权限判断与规则层级)。**可实现练习**：实现一个 `WordCountTool` 统计文件词数，并在权限系统中添加一条规则（如拒绝其读取特定类型文件），理解工具定义契约与拦截机制。\n\n**第 3 周：上下文与记忆引擎 (Context Engineering & Memory)**\n\n**明确主题**：掌握大模型长会话上下文的管理艺术，学习动静分离缓存、文件预取和动态摘要机制。**源码模块**：`src/context.ts` (系统上下文收集)、`src/services/compact/` (四级压缩管线)、`src/memdir/` 及 `src/services/extractMemories/` (自动记忆与持久化)。**可实现练习**：编写一个 `PreToolUse Hook`（前置钩子脚本），在每次文件写入前拦截并打印工具的输入参数，或者手动实现一段读取 `CLAUDE.md` 并拼接到 prompt 的上下文注入逻辑。\n\n**第 4 周：扩展机制与协议接入 (MCP & Skills)**\n\n**明确主题**：跨越本地界限，为 Agent 添加预设工作流（Skills）与外部环境（MCP）支持。**源码模块**：`src/services/mcp/` (MCP协议与客户端)、`src/skills/loadSkillsDir.ts` (Markdown 工作流加载与动态注册)。**可实现练习**：通过配置文件集成一个自定义的 MCP 服务器（如 `@modelcontextprotocol/server-github`），并在终端中让 Agent 成功调用该外部工具。\n\n**第 5 周：多 Agent 协作与生产化编排 (Sub-Agents & Orchestration)**\n\n**明确主题**：打破单兵作战瓶颈，学习 Fork、Team、Coordinator 等多 Agent 任务分发与状态同步机制。**源码模块**：`src/tools/AgentTool/` (子代理生成与路由)、`src/coordinator/` (多代理编排)、`src/utils/swarm/` (代理间协调与权限轮询)。**可实现练习**：手写一个极简的子代理编排器（Master 分发任务给两个 Worker 独立执行后，Master 收集最终结果并输出摘要），理解多代理间的上下文隔离。\n\n**第 6 周：高级系统与 IDE 桥接 (Bridge, Tasks & Advanced)**\n\n**明确主题**：掌握框架的远程执行与工程化集成，打通大模型与代码编辑器双向通讯的通道。**源码模块**：`src/bridge/` (IDE 消息桥接与权限回调)、`src/Task.ts` (后台任务引擎)、`src/cost-tracker.ts` (成本计算)。**可实现练习**：为 IDE（如 VS Code）接入添加一个新的 Bridge 消息类型协议，测试从本地终端向 IDE 发送或回传状态数据。\n\n\\--------------------------------------------------------------------------------\n\n与 Harness Engineering 三大主线的对应关系\n\n这条路线紧密契合了构建工业级 AI Agent 基础设施（Harness）的三个核心支柱：\n\n**Context Engineering（上下文工程）**：第 3 周和第 4 周专门训练如何将正确的信息在正确的时机喂给模型。从注入 `CLAUDE.md` 到构建 4 级压缩（Compact）管线，保障模型视野清晰且不超 Token 预算。**Architectural Constraints（架构约束 / Tool & Permission）**：第 1 周和第 2 周通过 Tool System 和 Permission System 的实战，建立硬性边界。让 Agent 放弃发散的建议，而是依赖带有校验、沙盒与权限拦截的具体“动作接口”工作。**Entropy Management（熵管理）**：第 5 周通过多 Agent 协作将任务拆解，辅以后台离线巩固（AutoDream）和 Plan->Work->Review 工作流，对抗代码系统在长周期运转下发生退化或事实矛盾的“熵增”。\n\n\\--------------------------------------------------------------------------------\n\n源码中值得反思的 3 个架构痛点\n\n尽管 Claude Code 极其庞大且健壮，但原作者在深度分析后也指出了以下几个不完美的架构痛点：\n\n**全局状态的过度滥用**：在 `src/bootstrap/state.ts` 中维护了一个包含 200 多个字段的超级全局状态对象。源码甚至留下了 `DO NOT ADD MORE STATE HERE` 的威慑性警告。在 50 万行规模的项目中，缺乏依赖注入和模块化的状态管理是一个巨大的维护隐患。**权限系统的认知负担过重**：安全漏斗虽强，但包含了 8 种规则来源、5 种权限模式、3 种正则匹配语法，以及极为复杂的优先级覆盖管道。这就导致用户和企业极难在脑海中理清“谁覆盖了谁”，即便有规则冲突预警功能（Shadowed Rule Detection）也只算是治标不治本。**BashTool 的复杂度高度集中**：单一的 `BashTool` 工具庞大到占用 18 个文件，甚至内置了 8 层安全检查流水线和长达 500 行的只读白名单校验。它承担了过多非核心的“安全卫士”职责；更好的设计应该是将命令审查功能彻底抽象剥离为一个全系统的“安全策略引擎”（OPA/Rego），让 BashTool 回归纯粹的执行层。"
    ]
  }
};
